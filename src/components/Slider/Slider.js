import React, { useState } from 'react';

const Slider = () => {
    const [active,setActive]=useState("FirstCard");

    return (
        <div>
            
        </div>
    );
};

export default Slider;